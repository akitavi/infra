apiVersion: v1
kind: Service
metadata:
  name: {{ include "t-api.fullname" . }}
  labels:
    {{- include "t-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "t-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 5000

